package zeab.k8readyservice.other

case class RewardLevel(
                        name:String = "moose",
                        points:Int = 1,
                        rewardCode:Int = 200
                      )
